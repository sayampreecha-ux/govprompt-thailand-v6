import test from 'node:test';import assert from 'node:assert/strict';import{createRequire}from'node:module';const require=createRequire(import.meta.url);const gate=require('../legal-authority-transition-gate.js');
test('blocks until all later-authority checks complete',()=>{const r=gate.evaluate({precedentReliedOn:true,precedentFactDate:'2017-01-01',currentFactDate:'2026-09-08',laterAuthoritySearchCompleted:true,laterAuthorities:[{id:'มท 0808.2/ว 0679',relevant:true,transitionResolved:false}]});assert.equal(r.pass,false);assert.equal(r.decisionLock,true);assert.ok(r.blockers.includes('rule-version-check-not-completed'));assert.ok(r.blockers.includes('contrary-evidence-check-not-completed'));assert.ok(r.blockers.includes('later-authority-effect-unresolved'));});
test('passes after timeline later-rule version transition and contrary checks resolve',()=>{const r=gate.evaluate({precedentReliedOn:true,precedentFactDate:'2017-01-01',currentFactDate:'2026-09-08',laterAuthoritySearchCompleted:true,ruleVersionCheckCompleted:true,contraryEvidenceCheckCompleted:true,laterAuthorities:[{id:'มท 0808.2/ว 0679',date:'2018-02-06',relevant:true,transitionResolved:true}]});assert.equal(r.pass,true);assert.equal(r.decisionLock,false);});
